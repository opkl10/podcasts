# 🚀 מדריך פריסה והעלאה לאחסון רגיל (uPress / cPanel / Apache / PHP)

מדריך זה מסביר כיצד להעלות ולהריץ את מערכת **CastFlow Studio** על כל שרת אחסון רגיל (כגון **uPress**, **cPanel**, **Hostinger**, **DirectAdmin**, או שרתי Apache/LiteSpeed/PHP).

---

## 🌟 שתי אפשרויות קלות לפריסה:

### אפשרות א': הרצה ישירה על אחסון uPress / cPanel עם PHP ו-Apache (מומלץ ופשוט)
1. **העלאת הקבצים**:
   - היכנסו למנהל הקבצים (File Manager) ב-uPress או ב-cPanel (או התחברו דרך FTP / FileZilla).
   - גשו לתיקיית היעד הראשית של הדומיין או הסאב-דומיין שלכם (למשל `public_html` או `public_html/studio`).
   - העלו את קובץ ה-ZIP של הפרויקט וחלצו אותו.

2. **קובץ הניתוב `.htaccess`**:
   - הקובץ `.htaccess` המצורף בפרויקט כבר מוגדר ומטפל אוטומטית בכל ניתובי ה-SPA (כגון `/guest`, `/episodes/123/studio`, `/remote-cam`) כך שלא תקבלו שגיאות 404.

3. **סקריפטים של PHP עבור בסיס הנתונים ו-WebRTC**:
   - בתוך תיקיית `php-server/` קיימים סקריפטים ייעודיים של PHP:
     - `api/db/index.php` – שמירת וטעינת הפרקים והפודקאסטים לקובץ JSON מאובטח בשרת.
     - `api/signaling/index.php` – תיווך WebRTC וחיבור אורחים ומצלמות מרחוק.
     - `api/network-ip/index.php` – זיהוי כתובות רשת.

---

### אפשרות ב': חיבור סאב-דומיין מיופרס לפריסה בחינם בענן (Vercel / Cloudflare)
אם אתם רוצים שהמערכת תרוץ על הדומיין שלכם ביופרס (למשל `studio.yourdomain.co.il`):
1. **היכנסו לניהול ה-DNS ב-uPress**:
   - הוסיפו רשומת **CNAME** חדשה:
     - **שם / מארח (Host)**: `studio`
     - **ערך (Target/Value)**: `cname.vercel-dns.com`
2. **העלאה ל-Vercel (חינם ב-100%)**:
   - התחברו ל-Vercel וחברו את תיקיית הפרויקט.
   - בהגדרות הדומיין (Domains) הוסיפו את `studio.yourdomain.co.il`.
   - תוך דקה האולפן פועל 24/7 עם תעודת SSL מאובטחת, קישורי אורחים קבועים ומהירות שיא!

---

## 🔒 אבטחה והרשאות בשרת:
- ודאו שלתיקיית `data/` יש הרשאות כתיבה (`chmod 755` או `chmod 775`) כדי ששרת ה-PHP יוכל לשמור את בסיס הנתונים `podcast_studio.db.json`.
- קובץ ה-`.htaccess` חוסם אוטומטית גישה ישירה לקובצי JSON וקובצי מערכת רגישים.
